#!/bin/sh
set -e

case "$1" in
    remove|upgrade)
        systemctl stop replayshield.service >/dev/null 2>&1 || true
        systemctl disable replayshield.service >/dev/null 2>&1 || true
        ;;
esac

exit 0
